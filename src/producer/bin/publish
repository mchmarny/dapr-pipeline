#!/bin/bash

set -o errexit
set -o pipefail

go mod tidy
go mod vendor

RELEASE_VERSION=v0.3.2

docker build \
  --build-arg APP_VERSION=$RELEASE_VERSION \
  -t mchmarny/producer:$RELEASE_VERSION \
  .

docker push mchmarny/producer:$RELEASE_VERSION
