#!/bin/bash

set -o errexit
set -o pipefail

export PORT=8082

go mod tidy
go mod vendor

dapr run --app-id processor \
         --app-port $PORT \
         --protocol http \
         go run handler.go main.go
