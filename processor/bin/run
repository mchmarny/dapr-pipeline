#!/bin/bash

set -o errexit
set -o pipefail

go mod tidy
go mod vendor

export PORT=8082

dapr run --app-id processor \
         --app-port $PORT \
         --protocol http \
         go run handler.go main.go
